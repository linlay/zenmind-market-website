# Sub-path for deploying under an existing domain (runtime, no rebuild needed).
# Leave empty for root deployment, or set to /<sub> (no trailing slash).
# Example: NGINX_BASE_PATH=/market  →  served at https://example.com/market/
NGINX_BASE_PATH=
BRAND=zenmind
