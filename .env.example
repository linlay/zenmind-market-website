# Sub-path for deploying under an existing domain.
# Leave empty for root deployment, or set to /<sub> (no trailing slash).
# Example: BASE_PATH=/market  →  served at https://example.com/market/
BASE_PATH=
BRAND=zenmind
